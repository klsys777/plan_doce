# 22 自由度灵巧手通信与控制架构设计

## 1. 项目背景

本项目为机器人灵巧手控制系统。

当前灵巧手共包含 **22 个电机**，分布于 5 根手指。典型情况下，一根手指约有 **4 个电机**，部分电机数量存在差异。

每个电机已经具备独立的电机驱动/控制器，外部控制系统主要负责：

* 下发电机运动控制指令
* 读取电机状态及反馈数据
* 管理多个电机之间的通信
* 实现整手协调控制
* 为后续轨迹规划、动作编排和上层控制提供统一接口

当前重点是电机只支持 **UART / TTL 串口**通信。

---

# 2. 核心需求

## 2.1 电机数量

* 总电机数量：**22 个**
* 手指数量：**5 根**
* 单根手指：约 **4 个电机**

典型结构：

```text
手指1：M1  M2  M3  M4
手指2：M5  M6  M7  M8
手指3：M9  M10 M11 M12
手指4：M13 M14 M15 M16
手指5：M17 M18 M19 M20
其他： M21 M22
```

具体电机分配可根据实际机构调整。

---

## 2.2 通信需求

当前优先考虑：

* UART
* TTL 电平
* 多电机通信
* 希望减少手指内部线束数量

核心目标不是单纯增加 UART 数量，而是：

> **在保证实时性、可靠性和成本可控的前提下，尽可能减少 22 个电机带来的线束复杂度。**

---

## 2.3 机械与线束需求

灵巧手内部空间有限，关节结构复杂，并且存在运动和弯折，因此需要重点考虑：

* 通信线数量
* 线束柔性
* 线缆布置空间
* 电机之间的串联/级联能力
* 插拔与维护便利性
* 电机更换便利性
* EMC/EMI 抗干扰能力

其中一个典型问题是：

```text
一根手指 4 个电机
```

如果每个电机都单独连接主控：

```text
主控
 ├── UART → Motor1
 ├── UART → Motor2
 ├── UART → Motor3
 └── UART → Motor4
```

会导致一根手指出现大量独立通信线，机械布线非常繁琐。

本方案采用 UART 扩展芯片，由主控通过 SPI 扩展出多路独立 UART，每个电机仍独占一路串口。

---

# 3. 总体架构设计目标

系统希望形成类似以下的分层架构：

```text
                    整手主控
                       │
                      SPI
                       │
                UART 扩展层
                       │
          ┌────────────┼────────────┐
          │            │            │
        手指1        手指2        手指3
          │            │            │
        电机群        电机群        电机群
          ...
```

系统可以分为三个逻辑层：

```text
系统控制层
      ↓
UART 扩展层
      ↓
电机关节层
```

其中：

### 系统控制层

负责：

* 整手控制
* 多手指协调
* 上层协议
* 状态管理
* 动作控制
* 通过 SPI 调度全部 UART 通道

### UART 扩展层

负责：

* 将主控 SPI 扩展为多路独立 UART
* 按手指或按电机群分配通道
* 为每个电机提供独立串口

### 电机关节层

负责：

* 电机闭环控制
* 位置/速度/电流控制
* 编码器采集
* 底层保护

最终设计目标：实现 22 自由度电机的同步控制，也可以自由控制不同自由度电机。

---

# 4. UART 扩展方案

## 4.1 基本思想

不增加手指 MCU，而是在主控 MCU 下面增加 UART 扩展器/分支器。

基本结构：

```text
                         主控 MCU
                     STM32G491RET6
                            │
                           SPI
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
      UART分支器           UART分支器          UART分支器
      CH9438F              CH9438F             CH9438F
        │                   │                   │
      多路UART             多路UART            多路UART
        │                   │                   │
      电机群               电机群              电机群
```

采用 **CH9438F**，每颗提供多路 UART。

---

## 4.2 按 3 个扩展器实现 22 路 UART

这是成本/资源比较直接的方案：

```text
CH9438F × 3

8 + 8 + 8 = 24 路 UART
```

22 个电机占用：

```text
22 路
```

剩余：

```text
2 路备用
```

架构：

```text
                    STM32G491RET6
                          │
                     SPI总线
                          │
          ┌───────────────┼───────────────┐
          │               │               │
       CH9438F         CH9438F         CH9438F
       UART×8           UART×8           UART×8
          │               │               │
        M1~M8           M9~M16          M17~M22
```

---

## 4.3 按手指划分的 5 分支变体

如果从机械结构出发，希望每根手指对应一个通信分支器，可以设计成：

```text
                        主控 MCU
                           │
                          SPI
                           │
       ┌─────────┬─────────┼─────────┬─────────┐
       │         │         │         │         │
      分支1     分支2     分支3      分支4      分支5
       │         │         │         │         │
     手指1      手指2      手指3      手指4      手指5
       │         │         │         │         │
      M1~M4     M5~M8     M9~M12    M13~M16   M17~M20
```

剩下 M21、M22 另行处理。

如果仍采用 **CH9438F**，单颗拥有 8 路 UART，而一根手指只有约 4 个电机，则每颗芯片会闲置约 4 路。单纯为了“每个手指一个 CH9438F”会存在资源浪费。

因此推荐默认采用 **3 × CH9438F**；若后续机械模块化要求更强，再评估是否改用更少通道的 UART 扩展芯片按手指布置。

---

## 4.4 方案优点

* 电机协议无需修改，每个电机仍使用独立 UART
* 主控只需管理 SPI 与通道映射，软件结构相对直接
* MCU 数量少，只需 1 颗整手主控
* UART 通道有余量，便于备用和扩展

## 4.5 方案缺点

* 每个电机仍独占一路 UART，手指内部线束较多
* 需要处理 3 颗 CH9438F 的 SPI 片选、时钟和通道调度
* 布线密度高，插拔与维护需要专门规划线束

---

# 5. 需要进一步确认的参数

在正式定型之前，需要确认以下几个关键参数：

1. **电机 UART 电平**

   * 5V TTL

2. **波特率**

   * 230400

3. **目标控制刷新率**

   * 100 Hz 或 1 kHz

这些参数最终决定 SPI 调度周期、CH9438F 通道配置，以及 22 路独立 UART 能否满足同步控制要求。

---

# 6. 现阶段候选器件

## 主控 MCU

**STM32G491RET6**

备选：

**STM32G474RET6**

如果后续需要更多计算、控制和通信资源，可进一步考虑该型号。

## UART 扩展器

**CH9438F**

数量：

```text
×3
```

理论 UART 数：

```text
8 × 3 = 24
```

可覆盖 22 个电机，并保留 2 路备用。

---

# 7. 总结

当前采用 UART 扩展架构：

```text
STM32G491RET6
        │
       SPI
        │
  CH9438F × 3
        │
  24 路独立 UART
        │
   22 个电机 + 2 路备用
```

该方案在电机必须独立 UART、协议不便修改的前提下，用最少的 MCU 数量把 22 路串口扩展出来。后续重点是 SPI 通道调度、线束布置，以及按手指或按电机群划分 CH9438F 通道映射。
