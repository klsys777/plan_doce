# 22 自由度灵巧手通信与控制架构设计

## 1. 项目背景

本项目为机器人灵巧手控制系统。

当前灵巧手共包含 **22 个电机**，分布于 5 根手指。典型情况下，一根手指约有 **4 个电机**，部分电机数量存在差异。

每个电机已经具备独立的电机驱动/控制器，外部控制系统主要负责：

* 下发电机运动控制指令
* 读取电机状态及反馈数据
* 管理多个电机之间的通信
* 实现整手协调控制
* 为后续轨迹规划、动作编排和上层控制提供统一接口

当前重点考虑使用 **UART / TTL 串口**作为电机通信接口。

---

# 2. 核心需求

## 2.1 电机数量

* 总电机数量：**22 个**
* 手指数量：**5 根**
* 单根手指：约 **4 个电机**

典型结构：

```text
手指1：M1  M2  M3  M4
手指2：M5  M6  M7  M8
手指3：M9  M10 M11 M12
手指4：M13 M14 M15 M16
手指5：M17 M18 M19 M20
其他： M21 M22
```

具体电机分配可根据实际机构调整。

---

## 2.2 通信需求

当前优先考虑：

* UART
* TTL 电平
* 多电机通信
* 希望减少手指内部线束数量

核心目标不是单纯增加 UART 数量，而是：

> **在保证实时性、可靠性和成本可控的前提下，尽可能减少 22 个电机带来的线束复杂度。**

---

## 2.3 机械与线束需求

灵巧手内部空间有限，关节结构复杂，并且存在运动和弯折，因此需要重点考虑：

* 通信线数量
* 线束柔性
* 线缆布置空间
* 电机之间的串联/级联能力
* 插拔与维护便利性
* 电机更换便利性
* EMC/EMI 抗干扰能力

其中一个典型问题是：

```text
一根手指 4 个电机
```

如果每个电机都单独连接主控：

```text
主控
 ├── UART → Motor1
 ├── UART → Motor2
 ├── UART → Motor3
 └── UART → Motor4
```

会导致一根手指出现大量独立通信线，机械布线非常繁琐。

因此希望探索：

> **4 个电机通过一条通信链路进行级联/总线通信。**

---

# 3. 总体架构设计目标

系统希望形成类似以下的分层架构：

```text
                    整手主控
                       │
                       │
                ┌──────┴──────┐
                │             │
              手指1          手指2
                │             │
              电机群          电机群
                │
              手指3
                │
              电机群
                ...
```

系统可以分为三个逻辑层：

```text
系统控制层
      ↓
手指控制层
      ↓
电机关节层
```

其中：

### 系统控制层

负责：

* 整手控制
* 多手指协调
* 上层协议
* 状态管理
* 动作控制

### 手指控制层

负责：

* 本手指 4 个左右电机管理
* 本地通信调度
* 电机状态采集
* 故障处理
* 局部实时控制

### 电机关节层

负责：

* 电机闭环控制
* 位置/速度/电流控制
* 编码器采集
* 底层保护

####最终设计目标：实现22自由度电机的同步控制，也可以自由控制不同自由度电机。
---

# 4. 方案一：5 MCU 分布式方案

## 4.1 基本架构

采用：

* 1 个整手主控 MCU
* 5 个手指 MCU
* 每个手指 MCU 管理本手指的电机

推荐架构：

```text
                         整手主控
                    STM32G491RET6
                           │
          ┌────────────────┼────────────────┐
          │                │                │
        手指1            手指2            手指3
          │                │                │
   STM32G431CBU6    STM32G431CBU6    STM32G431CBU6
          │                │                │
       UART×4           UART×4           UART×4
       ││││             ││││             ││││
      M1 M2 M3 M4      M5 M6 M7 M8      M9...
```

手指 4、手指 5同理。

---

## 4.2 推荐芯片

### 整手主控

推荐：

**STM32G491RET6**

特点：

* 性能余量较充足
* UART/USART 等通信资源丰富
* SPI 等接口资源丰富
* 适合作为整手控制器

也可以考虑：

**STM32G474RET6**

如果后续需要更多计算、控制和通信资源，可进一步考虑该型号。

### 手指 MCU

推荐：

**STM32G431CBU6 × 5**

每根手指配置一个 MCU。

---

## 4.3 结构

```text
                         STM32G491
                              │
                    手指级通信总线
                              │
       ┌──────────┬───────────┼───────────┬──────────┐
       │          │           │           │          │
      MCU1       MCU2        MCU3        MCU4       MCU5
       │          │           │           │          │
    UART×4      UART×4      UART×4      UART×4     UART×4
       │          │           │           │          │
    M1~M4       M5~M8       M9~M12     M13~M16    M17~M20
```

剩余的 M21~M22 可以根据实际机构：

* 分配给某个手指 MCU
* 放到手掌局部控制器
* 单独增加通信接口

---

## 4.4 方案优点

### 1. 非常适合机械模块化

每根手指可以设计成独立模块：

```text
手指机械结构
     +
手指控制板
     +
4个电机
```

### 2. 单个 MCU 只负责局部电机

软件压力较小。

例如手指 MCU 只处理：

```text
Motor1
Motor2
Motor3
Motor4
```

而主控只管理：

```text
Finger1
Finger2
Finger3
Finger4
Finger5
```

### 3. 线束明显减少

主控不需要直接拉 22 路 UART 到所有电机。

---

## 4.5 方案缺点

### 1. MCU 数量增加

需要：

```text
主控 MCU × 1
手指 MCU × 5
```

总计：

**6 个 MCU**

### 2. PCB 数量增加

至少：

* 1 块主控板
* 5 块手指控制板

### 3. 软件架构更复杂

需要处理：

* 主控与手指 MCU 通信
* 手指 MCU 状态同步
* 多节点升级
* 故障管理

---

# 5. 方案二：5 个 UART 分支器方案

## 5.1 基本思想

不增加 5 个手指 MCU，而是在主控 MCU 下面增加 UART 扩展器/分支器。

基本结构：

```text
                         主控 MCU
                     STM32G491RET6
                            │
                           SPI
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
      UART分支器           UART分支器          UART分支器
      CH9438F              CH9438F             CH9438F
        │                   │                   │
      多路UART             多路UART            多路UART
        │                   │                   │
      电机群               电机群              电机群
```

如果采用：

**CH9438F**

每颗提供多路 UART。

---

## 5.2 按 3 个分支器实现 22 路 UART

这是之前讨论过的成本/资源比较直接的方案：

```text
CH9438F × 3

8 + 8 + 8 = 24 路 UART
```

22 个电机占用：

```text
22 路
```

剩余：

```text
1 路备用
```

架构：

```text
                    STM32G491RET6
                          │
                     SPI总线
                          │
          ┌───────────────┼───────────────┐
          │               │               │
       CH9438F         CH9438F         CH9438F
       UART×8           UART×8           UART×8
          │               │               │
        M1~M8           M9~M16          M17~M22
```

---

# 6. “5 个分支器”方案

如果从机械结构出发，希望：

> **每根手指对应一个通信分支器**

可以设计成：

```text
                        主控 MCU
                           │
                          SPI
                           │
       ┌─────────┬─────────┼─────────┬─────────┐
       │         │         │         │         │
      分支1     分支2     分支3      分支4      分支5
       │         │         │         │         │
     手指1      手指2      手指3      手指4      手指5
       │         │         │         │         │
      M1~M4     M5~M8     M9~M12    M13~M16   M17~M20
```

剩下 3 个电机另行处理。

---

## 6.1 这种方案的问题

如果采用：

**CH9438F**

单颗拥有较多 UART 通道。

但是一个手指只有约：

```text
4 个电机
```

那么：

```text
8 UART
↓
只使用4路
↓
剩余4路闲置
```

因此如果单纯为了“每个手指一个 CH9438F”，会存在资源浪费。

---

# 7. 方案三：手指内部 TTL UART 级联

这是当前非常值得重点研究的方案。

核心目标：

> **一个手指 4 个电机，只通过一条通信链路完成通信。**

---

## 7.1 基本结构

```text
                    手指控制器
                         │
                         │ TTL UART
                         ▼
                      Motor1
                         │
                         │ UART
                         ▼
                      Motor2
                         │
                         │ UART
                         ▼
                      Motor3
                         │
                         │ UART
                         ▼
                      Motor4
```

这样机械上就可以实现：

```text
控制器
  │
  └──────── 一条通信链 ────────→ M1 → M2 → M3 → M4
```

相比：

```text
控制器
 ├────────→ M1
 ├────────→ M2
 ├────────→ M3
 └────────→ M4
```

线束数量明显下降。

---

# 8. TTL 级联的关键技术问题

需要特别注意：

> **普通 UART 并不是天然的多节点总线。**

不能简单地将多个 UART 的 TX/RX 直接并联。

普通 UART 更接近：

```text
TX ───────── RX
RX ───────── TX
```

而多个设备共享总线，需要解决：

* 地址识别
* 数据转发
* 总线占用
* 发送冲突
* 节点故障
* CRC 校验

因此真正的 TTL 级联应该设计成“多点通信协议”，而不是简单并线。

---

# 9. TTL 级联的一种实现

每个电机控制器具有：

```text
UART IN
UART OUT
```

内部：

```text
                 UART IN
                    │
                    ▼
             ┌─────────────┐
             │ Motor MCU   │
             │             │
             │ 地址判断    │
             │ 协议解析    │
             │ 数据转发    │
             └──────┬──────┘
                    │
                    ▼
               Motor Control
                    │
                    ▼
                 UART OUT
```

协议例如：

```text
┌────┬────┬──────┬──────┬────┐
│ ID │CMD │ DATA │ CRC  │ ...│
└────┴────┴──────┴──────┴────┘
```

例如：

```text
ID = 1 → Motor1
ID = 2 → Motor2
ID = 3 → Motor3
ID = 4 → Motor4
```

每个电机只处理属于自己的数据，其余数据继续向后转发。

---

# 10. 三种方案的核心对比

| 项目        |  5 MCU | 3×UART分支器 |       TTL级联 |
| --------- | -----: | --------: | ----------: |
| 主控 MCU    |      1 |         1 |           1 |
| 手指 MCU    |      5 |         0 |          可选 |
| UART 扩展芯片 |      0 |         3 |        0/可选 |
| 电机独立 UART |      是 |         是 |           否 |
| 单手指线束     |     中等 |        很多 |       **少** |
| 软件复杂度     |      中 |         中 |      **较高** |
| 硬件成本      |     较高 |        较低 |       取决于实现 |
| 模块化       | **很好** |        一般 |          较好 |
| 机械布线      |      好 |        较差 |      **最好** |
| 可扩展性      |      好 |         好 |           好 |
| EMC 抗干扰   | 取决于物理层 |    取决于物理层 | **TTL相对较弱** |
| 开发难度      |      中 |         中 |      **较高** |

---

# 11. 推荐的系统演进路线

建议不要一开始就把所有问题一次解决。

可以分三步验证。

## 第一阶段：验证单手指

先做：

```text
MCU
 │
 └── TTL UART
       │
      M1
       │
      M2
       │
      M3
       │
      M4
```

验证：

* 4 个电机是否可以级联
* 波特率
* 控制周期
* 数据吞吐量
* CRC
* 电机地址机制
* 转发延迟
* 通信稳定性

---

## 第二阶段：验证五指

形成：

```text
                     主控
                       │
             ┌─────────┼─────────┐
             │         │         │
           Finger1   Finger2   Finger3
             │         │         │
           M1~M4     M5~M8     M9~M12

             │         │
           Finger4   Finger5
             │         │
          M13~M16   M17~M20
```

---

## 第三阶段：整手统一通信

最终形成：

```text
                  整手主控
               STM32G491RET6
                      │
          ┌───────────┼───────────┐
          │           │           │
       Finger1     Finger2      Finger3
          │           │           │
       M1→M2→M3→M4  M5→M6→M7→M8  ...
          
          Finger4                  Finger5
             │                        │
       M13→M14→M15→M16        M17→M18→M19→M20
```

M21~M22 根据实际机械布局加入相应节点。

---

# 12. 当前建议的优先级

当前阶段建议重点研究：

### 第一优先：TTL 级联

因为它最直接解决目前最核心的机械问题：

> **一个手指 4 个电机，如何避免 4 组独立通信线。**

### 第二优先：5 MCU 分布式方案

作为工程上比较稳妥的备选：

```text
STM32G491RET6 × 1
+
STM32G431CBU6 × 5
```

优点是模块化明显、调试方便。

### 第三优先：UART 分支器

如果最终确认：

* 电机必须独立 UART
* 电机协议无法修改
* 不适合级联

则采用：

```text
STM32G491RET6
+
CH9438F × 3
```

实现 24 路 UART。

---

# 13. 最终需要进一步确认的参数

在正式定型之前，需要确认以下几个关键参数：

1. **电机 UART 电平**

   * 5V TTL

2. **波特率**

   * 230400

5. **目标控制刷新率**

   * 100 Hz 或 1 kHz

这些参数最终决定：

> **TTL 级联是否真正可行，以及应该采用 5 MCU、UART 分支器还是总线型架构。**

---

# 14. 现阶段候选器件

## 主控 MCU

**STM32G491RET6**

备选：

**STM32G474RET6**

## 手指 MCU

**STM32G431CBU6**

数量：

```text
×5
```

## UART 扩展器

**CH9438F**

数量：

```text
×3
```

理论 UART 数：

```text
8 × 3 = 24
```

可覆盖 22 个电机，并保留 1 路备用。

---

# 15. 总结

当前设计实际上有三个方向：

```text
             22个电机
                 │
       ┌─────────┼──────────┐
       │         │          │
      5 MCU     UART扩展    TTL级联
       │         │          │
    模块化强     简单直接     线束最少
       │         │          │
    成本较高     线束较多     协议设计复杂
```

对于灵巧手这种**空间紧凑、线束弯折频繁**的设备，通信架构不能只考虑“UART 数量够不够”，还必须把**线束结构、节点拓扑和维护性**作为核心指标。

因此下一阶段建议优先验证：

> **“4 个电机能否通过 TTL UART 级联成为一个手指级通信链路。”**

验证成功后，再确定整手最终采用 **5 MCU + TTL 级联**，还是其他架构。
