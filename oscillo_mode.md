---
name: 示波器触发模式
overview: motor_tool 触发示波器当前实现说明。上位机下发显示通道、预触发比例、A/B 条件关系和阈值；下位机触发后按 0x20 分包回传采样数据；上位机收齐结束帧后一次性绘制，等待期间支持手动取消，并支持 Auto、清除、拖拽、框选缩放和曲线悬浮数据显示。
isProject: false
---

# 示波器触发模式

## 功能概览

触发示波器用于观察满足指定条件时的一段采样波形。它与连续示波器不同：连续示波器边收边滚动刷新，触发示波器是在下位机触发并回传数据后，上位机一次性绘制整段波形。

当前上位机支持：

- 选择显示通道：`ia`、`ib`、`ic`、`iq`、`id`、`vel`、`pos`。
- 设置预触发比例：`0~100%`。默认 `0%`，表示不保留触发前窗口。
- 设置条件关系：`A`、`B`、`A&&B`、`A||B`。
- 设置 A/B 两组触发对象：`null`、`ia`、`ib`、`ic`、`iq`、`id`、`vel`、`pos`、`error`。
- 设置触发条件：`>`、`<`、`=`、`>=`、`<=`。
- 设置触发阈值：输入框保留两位小数，例如 `10.00`。
- 触发采集后一次性显示波形。
- 等待触发期间可手动取消本次等待。
- `Auto` 自动适配坐标轴。
- `清除` 清空当前触发波形。
- 鼠标左键框选缩放，右键拖拽平移。
- 鼠标靠近曲线时显示当前点的横坐标和纵坐标。

## UI 行为

触发示波器页面由左侧波形显示区和右侧触发面板组成。

右侧触发面板包含：

| 控件 | 说明 |
|------|------|
| 显示通道 | 选择触发后回传/显示哪一路数据，默认 `ia` |
| 预触发设定 | 百分比输入，范围 `0~100%`，默认 `0%` |
| 条件关系 | 选择 `A`、`B`、`A&&B` 或 `A||B`；未参与的条件组会置灰 |
| 条件 A/B - 对象 | 选择触发对象，默认 `ia` |
| 条件 A/B - 条件 | 选择比较条件，默认 `>` |
| 条件 A/B - 阈值 | 触发阈值，默认 `0.00`，输入后自动格式化为两位小数 |
| 触发/取消 | 空闲时下发触发配置并等待数据；等待时取消本次等待 |
| Auto | 按当前数据自动适配坐标轴 |
| 清除 | 清空当前触发波形并恢复空闲状态 |
| 状态 | 显示空闲、等待、完成或错误 |

显示通道与触发对象是两类设置：显示通道决定回传/绘制哪一路数据；触发对象决定下位机用哪一路信号判断触发条件。

阈值输入会在编辑结束时格式化，例如输入 `100` 后显示为 `100.00`。点击“触发”前也会再次格式化，避免输入框尚未失焦时直接下发未规范化的文本。下发前阈值会执行：

```text
custom_value = round(输入阈值 * 100)
```

特殊对象规则：

- `null`：无条件触发。下位机收到该触发对象后直接触发，忽略对应条件和阈值。
- `error`：错误码触发。上位机会把对应条件强制切到 `=`；输入仍按 `round(input * 100)` 下发。例如输入 `5` 表示等待错误码 5，输入 `1000` 表示任意非 0 错误码。

等待触发时，“触发”按钮会切换为“取消等待”。点击“取消等待”只取消上位机当前等待并恢复空闲显示，不额外下发取消帧；下一次点击“触发”会重新初始化接收缓存。

## 协议说明

触发示波器使用 `UART_FRAME_PARA_UPDATE_TRIG_CFG = 0x20`。

### 触发配置帧

上位机下发触发配置：

```text
AA 55 11 20 MOT_ID display_channel pre_trigger trigger_relation trigger_channel_A trigger_condition_A custom_value_A[4] trigger_channel_B trigger_condition_B custom_value_B[4] CHK_L CHK_H
```

payload 字段共 15 字节：

| 偏移 | 字段 | 长度 | 说明 |
|------|------|------|------|
| 0 | `display_channel` | 1 | 显示/采样通道，从 0 开始 |
| 1 | `pre_trigger` | 1 | 预触发比例，单位 `%`，范围 `0~100` |
| 2 | `trigger_relation` | 1 | 条件关系，从 0 开始 |
| 3 | `trigger_channel_A` | 1 | A 触发对象 |
| 4 | `trigger_condition_A` | 1 | A 触发条件 |
| 5~8 | `custom_value_A` | 4 | A 阈值放大 100 后的 `int32`，小端 |
| 9 | `trigger_channel_B` | 1 | B 触发对象 |
| 10 | `trigger_condition_B` | 1 | B 触发条件 |
| 11~14 | `custom_value_B` | 4 | B 阈值放大 100 后的 `int32`，小端 |

显示通道：

| 值 | 通道 |
|----|------|
| 0 | `ia` |
| 1 | `ib` |
| 2 | `ic` |
| 3 | `iq` |
| 4 | `id` |
| 5 | `vel` |
| 6 | `pos` |

触发对象：

| 值 | 对象 | 说明 |
|----|------|------|
| 0 | `null` | 无条件触发 |
| 1 | `ia` | A 相电流 |
| 2 | `ib` | B 相电流 |
| 3 | `ic` | C 相电流 |
| 4 | `iq` | q 轴电流 |
| 5 | `id` | d 轴电流 |
| 6 | `vel` | 速度 |
| 7 | `pos` | 位置 |
| 8 | `error` | 错误码 |

条件关系：

| 值 | 关系 |
|----|------|
| 0 | `A` |
| 1 | `B` |
| 2 | `A&&B` |
| 3 | `A||B` |

触发条件：

| 值 | 条件 |
|----|------|
| 0 | `>` |
| 1 | `<` |
| 2 | `=` |
| 3 | `>=` |
| 4 | `<=` |

预触发规则：

```text
pre_count = 1024 * pre_trigger / 100
post_count = 1024 - pre_count
```

上传曲线按时间顺序排列，`seq = 0` 是最旧点；触发位置约在 `seq = pre_count`。例如 `pre_trigger = 25` 时，前 256 点为触发前数据，后 768 点为触发点及触发后数据。当前上位机按 `seq` 绘制时间轴，不额外绘制触发位置标线。

例如显示 `iq`，预触发 `25%`，触发条件为 `iq > 20.00 && vel < 100.00`：

| 字段 | 值 |
|------|----|
| `display_channel` | `3`，即 `iq` |
| `pre_trigger` | `25` |
| `trigger_relation` | `2`，即 `A&&B` |
| `trigger_channel_A` | `4`，即 `iq` |
| `trigger_condition_A` | `0`，即 `>` |
| `custom_value_A` | `2000`，即 `D0 07 00 00` |
| `trigger_channel_B` | `6`，即 `vel` |
| `trigger_condition_B` | `1`，即 `<` |
| `custom_value_B` | `10000`，即 `10 27 00 00` |

### 数据反馈帧

下位机触发后继续使用 `0x20` 回传数据。普通数据帧由 `packet_idx + data[64]` 组成，结束帧使用 `packet_idx = 0xFF`。

普通数据帧：

```text
5A A5 43 20 MOT_ID packet_idx data[64] CHK_L CHK_H
```

| 字段 | 长度 | 说明 |
|------|------|------|
| `packet_idx` | 1 | 包序号，从 0 开始 |
| `data` | 64 | 固定 64 字节采样数据 |

`data[64]` 按 4 字节一组解析，每帧 16 个采样点：

| 组内偏移 | 字段 | 长度 | 说明 |
|----------|------|------|------|
| 0~1 | `seq` | 2 | 采样序号，`u16`，小端 |
| 2~3 | `value` | 2 | 采样值，`s16`，小端 |

默认共 64 包，每包 16 点，总计约 1024 点。如果收到更多包，上位机会按最大包号扩展解析范围。缺失的数据包会在图中以红色散点标记。

结束帧：

```text
5A A5 03 20 MOT_ID FF CHK_L CHK_H
```

上位机收到 `packet_idx = 0xFF` 后，拼接已收到的数据并绘制曲线。

## 数据显示规则

触发示波器当前只绘制触发回传曲线到第 0 条 graph。

坐标轴：

| 坐标轴 | 显示 |
|--------|------|
| X 轴 | `时间 (ms)` |
| Y 轴 | `采样值` |

采样间隔固定为 40 us，因此横坐标由采样序号换算为毫秒：

```text
x(ms) = seq * 0.04
```

纵坐标由原始采样值换算：

```text
y = value / 10.0
```

因此 1024 个点的时间跨度约为：

```text
1024 * 40 us = 40.96 ms
```

## 图表交互

| 操作 | 作用 |
|------|------|
| 左键框选 | 缩放到框选区域 |
| 右键拖拽 | 平移坐标轴 |
| 鼠标靠近曲线 | 显示当前最近点的 `x` 和 `y` |
| Auto | 自动适配当前数据范围 |
| 清除 | 清空触发数据 |

鼠标悬浮提示只对可见曲线生效，不对红色缺失点散点生效。拖拽或框选时不会显示悬浮提示，避免干扰操作。

## 状态与异常

| 状态 | 含义 |
|------|------|
| 空闲 | 未触发或已清除 |
| 等待 | 已下发触发配置，等待下位机触发/回传；可点击“取消等待”手动结束等待 |
| 完成 | 收到结束帧并完成绘制 |
| 错误 | 串口未打开、模式冲突或数据长度错误 |

注意事项：

1. 触发示波器不能与连续发送模式、连续示波器模式同时运行。
2. 数据反馈帧中 `packet_idx` 后直接跟 `data[64]`，不包含 `errorCode`。
3. 上位机按 `packet_idx` 拼接数据，丢包时会生成缺失点标记。
4. 协议字段和触发对象编号需要上下位机保持一致。
5. 当前取消逻辑是上位机本地取消，不改变下位机触发配置；现有协议没有触发批次序号，若取消后立刻重新触发，仍无法从协议上区分迟到旧数据和新数据。如果后续协议增加取消帧或批次号，可在点击“取消等待”时补充下发或校验。
