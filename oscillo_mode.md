---
name: 示波器触发模式
overview: motor_tool 触发示波器当前实现说明。上位机下发触发通道、条件、阈值；下位机触发后按 0x20 分包回传采样数据；上位机收齐结束帧后一次性绘制，等待期间支持手动取消，并支持 Auto、清除、拖拽、框选缩放和曲线悬浮数据显示。
isProject: false
---

# 示波器触发模式

## 功能概览

触发示波器用于观察满足指定条件时的一段采样波形。它与连续示波器不同：连续示波器边收边滚动刷新，触发示波器是在下位机触发并回传数据后，上位机一次性绘制整段波形。

当前上位机支持：

- 设置触发通道：`ia`、`ib`、`ic`、`iq`、`id`、`vel`、`pos`。
- 设置触发条件：`>`、`<`、`=`、`>=`、`<=`。
- 设置触发阈值：输入框保留两位小数，例如 `10.00`。
- 触发采集后一次性显示波形。
- 等待触发期间可手动取消本次等待。
- `Auto` 自动适配坐标轴。
- `清除` 清空当前触发波形。
- 鼠标左键框选缩放，右键拖拽平移。
- 鼠标靠近曲线时显示当前点的横坐标和纵坐标。

## UI 行为

触发示波器页面由左侧波形显示区和右侧触发面板组成。

右侧触发面板包含：

| 控件 | 说明 |
|------|------|
| 通道 | 选择触发通道，默认第一个 `ia` |
| 条件 | 选择触发条件，默认第一个 `>` |
| 阈值 | 触发阈值，默认 `0.00`，输入后自动格式化为两位小数 |
| 触发/取消 | 空闲时下发触发配置并等待数据；等待时取消本次等待 |
| Auto | 按当前数据自动适配坐标轴 |
| 清除 | 清空当前触发波形并恢复空闲状态 |
| 状态 | 显示空闲、等待、完成或错误 |

阈值输入会在编辑结束时格式化，例如输入 `100` 后显示为 `100.00`。点击“触发”前也会再次格式化，避免输入框尚未失焦时直接下发未规范化的文本。

等待触发时，“触发”按钮会切换为“取消”。点击“取消”只取消上位机当前等待并恢复空闲显示，不额外下发取消帧；取消后到下一次触发前，若下位机仍回传旧数据，上位机会忽略这批迟到数据。下一次点击“触发”会重新初始化接收缓存。

## 协议说明

触发示波器使用 `UART_FRAME_PARA_UPDATE_TRIG_CFG = 0x20`。

### 触发配置帧

上位机下发触发配置：

```text
AA 55 LEN 20 MOT_ID trigger_channel trigger_condition custom_value[4] CHK_H CHK_L
```

payload 字段：

| 偏移 | 字段 | 长度 | 说明 |
|------|------|------|------|
| 0 | `trigger_channel` | 1 | 触发通道，从 0 开始 |
| 1 | `trigger_condition` | 1 | 触发条件，从 0 开始 |
| 2~5 | `custom_value` | 4 | 阈值放大 100 后的 `int32`，小端 |

触发通道：

| 值 | 通道 |
|----|------|
| 0 | `ia` |
| 1 | `ib` |
| 2 | `ic` |
| 3 | `iq` |
| 4 | `id` |
| 5 | `vel` |
| 6 | `pos` |

触发条件：

| 值 | 条件 |
|----|------|
| 0 | `>` |
| 1 | `<` |
| 2 | `=` |
| 3 | `>=` |
| 4 | `<=` |

阈值转换规则：

```text
custom_value = round(输入阈值 * 100)
```

例如 `iq > 10.00`：

| 字段 | 值 |
|------|----|
| `trigger_channel` | `3` |
| `trigger_condition` | `0` |
| `custom_value` | `1000`，即 `E8 03 00 00` |

### 数据反馈帧

下位机触发后继续使用 `0x20` 回传数据。普通数据帧由 `packet_idx + data[64]` 组成，结束帧使用 `packet_idx = 0xFF`。

普通数据帧：

```text
5A A5 43 20 MOT_ID packet_idx data[64] CHK_L CHK_H
```

| 字段 | 长度 | 说明 |
|------|------|------|
| `packet_idx` | 1 | 包序号，从 0 开始 |
| `data` | 64 | 固定 64 字节采样数据 |

`data[64]` 按 4 字节一组解析，每帧 16 个采样点：

| 组内偏移 | 字段 | 长度 | 说明 |
|----------|------|------|------|
| 0~1 | `seq` | 2 | 采样序号，`u16`，小端 |
| 2~3 | `value` | 2 | 采样值，`s16`，小端 |

默认共 64 包，每包 16 点，总计约 1024 点。如果收到更多包，上位机会按最大包号扩展解析范围。缺失的数据包会在图中以红色散点标记。

结束帧：

```text
5A A5 03 20 MOT_ID FF CHK_L CHK_H
```

上位机收到 `packet_idx = 0xFF` 后，拼接已收到的数据并绘制曲线。

## 数据显示规则

触发示波器当前只绘制触发回传曲线到第 0 条 graph。

坐标轴：

| 坐标轴 | 显示 |
|--------|------|
| X 轴 | `时间 (ms)` |
| Y 轴 | `采样值` |

采样间隔固定为 40 us，因此横坐标由采样序号换算为毫秒：

```text
x(ms) = seq * 0.04
```

纵坐标由原始采样值换算：

```text
y = value / 10.0
```

因此 1024 个点的时间跨度约为：

```text
1024 * 40 us = 40.96 ms
```

## 图表交互

| 操作 | 作用 |
|------|------|
| 左键框选 | 缩放到框选区域 |
| 右键拖拽 | 平移坐标轴 |
| 鼠标靠近曲线 | 显示当前最近点的 `x` 和 `y` |
| Auto | 自动适配当前数据范围 |
| 清除 | 清空触发数据 |

鼠标悬浮提示只对可见曲线生效，不对红色缺失点散点生效。拖拽或框选时不会显示悬浮提示，避免干扰操作。

## 状态与异常

| 状态 | 含义 |
|------|------|
| 空闲 | 未触发或已清除 |
| 等待 | 已下发触发配置，等待下位机触发/回传；可点击“取消”手动结束等待 |
| 完成 | 收到结束帧并完成绘制 |
| 错误 | 串口未打开、模式冲突或数据长度错误 |

注意事项：

1. 触发示波器不能与连续发送模式、连续示波器模式同时运行。
2. 数据反馈帧中 `packet_idx` 后直接跟 `data[64]`，不再包含 `errorCode`。
3. 上位机按 `packet_idx` 拼接数据，丢包时会生成缺失点标记。
4. 协议字段和触发通道编号需要上下位机保持一致。
5. 当前取消逻辑是上位机本地取消，不改变下位机触发配置；现有协议没有触发批次序号，若取消后立刻重新触发，仍无法从协议上区分迟到旧数据和新数据。如果后续协议增加取消帧或批次号，可在点击“取消”时补充下发或校验。
